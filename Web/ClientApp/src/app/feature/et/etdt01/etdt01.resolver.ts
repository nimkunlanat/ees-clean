import { ResolveFn } from '@angular/router';

export const etdt01Resolver: ResolveFn<boolean> = (route, state) => {
  return true;
};
export const etdt01SkillResolver: ResolveFn<boolean> = (route, state) => {
  return true;
};
