import { EntityBase } from "../entityBase"

export class ProgramLabel extends EntityBase {
    programCode: string
    fieldName: string
    languageCode: string
    labelName: string
    systemCode: string
    moduleCode: string
}